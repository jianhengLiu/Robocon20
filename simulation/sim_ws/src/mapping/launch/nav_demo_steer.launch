<launch>

    <param name="use_sim_time" value="false"/>

    <node name="steerwheel" pkg="vrep_pkg" type="steerwheel" output="screen">
    </node>

    <!-- 运行move_base节点 -->
    <include file="$(find mapping)/launch/move_base_dwa.launch"/>

    <!-- 运行rviz -->
    <node pkg="rviz" type="rviz" name="rviz">
    </node>
</launch>

